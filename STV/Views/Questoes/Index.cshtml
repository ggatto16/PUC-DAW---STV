@model IEnumerable<STV.Models.Questao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create", null, new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Alternativa.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Atividade.Idatividade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Alternativa.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Atividade.Idatividade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Alterar", "Edit", new { id=item.Idquestao }) |
            @Html.ActionLink("Details", "Details", new { id=item.Idquestao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Idquestao })
        </td>
    </tr>
}

</table>
